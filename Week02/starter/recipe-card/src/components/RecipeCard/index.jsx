// TODO (in class): paste your recipe card HTML from homework in here and
// convert it to JSX.
//
// Reminders when converting HTML -> JSX:
//   class      -> className
//   for        -> htmlFor
//   <br>       -> <br />        (every tag must close)
//   style="…"  -> style={{…}}   (an object, not a string)
//   one component returns ONE parent element (or a <> … </> fragment)
//
// To use the image:
//   import PANCAKE from '../../assets/pancake.jpg'
//   <img src={PANCAKE} alt="buttermilk pancakes" />

export default function RecipeCard() {
  return <div>My recipe card goes here.</div>
}
